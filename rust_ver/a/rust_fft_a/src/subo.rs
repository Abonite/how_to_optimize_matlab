pub fn subo(source_points: Vec<f64>, gap: usize) -> Vec<Complex> {
    loop_t: usize = source_points.len() / 2;
    out_points: Vec<Complex> = vec![Complex::new(0.0, 0.0); source_points.len()];

    for k in 0..loop_t {
        N = 2 * gap;
        W = ;
        i_a = source_points[k];
        i_b = source_points[k + gap];
    }
}